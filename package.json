{
  "name": "bosh-action",
  "version": "1.0.0",
  "private": true,
  "description": "Run a workspace script in a local BOSH integration environment.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node --check main.js",
    "test": "node --test"
  }
}
